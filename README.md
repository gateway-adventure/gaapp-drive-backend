# GAAPP Google Drive Backend

Backend proxy for Unity WebGL → Google Drive.
Full CRUD support.

## Setup
1. Extract ZIP
2. Run: npm install
3. Run locally: npm start
4. Deploy to Vercel: vercel deploy

## API Endpoints
- POST /api/push
- POST /api/pull
- POST /api/delete
- POST /api/folders/create
- POST /api/folders/find